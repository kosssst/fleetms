export interface LoginFormValues {
  email: string;
  password?: string;
}

export interface RegisterFormValues {
    email: string;
    password?: string;
    firstName: string;
    lastName: string;
}
